# DeepLearning

This repository contains multiple neural networks implemented from scratch in julia. Besides the model implementation, I also wrote a few optimization methods to train the models in "train.jl". The methods include gradient descent, stochastic gradient descent, Armijo-Wolfe conditions and ADAM optimization.

A few examples where I trained the models on actual datasets can be found in "examples".